var App = (function (scope, config) {
    var init = function () {

    };

    return {
        ambito: 'nacional',
        categoria: 'P01',
        ubigeo: '00',

        /* Metodos */
        init: init
    }
})(window, AppConfig());